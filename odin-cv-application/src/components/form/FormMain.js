import { Component } from "react";

class FormMain extends Component {
  constructor(props) {
    super(props);
  };

  render() {
    return (
      <div>
        FormMain
      </div>
    );
  };
}

export default FormMain;